#include "main.h"

#include <cmath>
#include "api/print.h"
#include "api/primitives/string.h"

#include "alt.h"

void bla() {
	int ffdfdf = 32;
}

test

int a = 32;
int b = 0;
auto fds;
std::string fdsd = "fdsklfds";
int aaa = 0;
std::string blabla = "";

int main() {
	b = bla2;
	{
		d.df = 43;
		if(bfkld) {
			print("FUDFKDL");
		}
	}
	{
		if(a == 32) {
			a++;
		}
	}
	if(a == 32) {
		b::gf++;
	}
	print("fd");
	get_string_length(fdsd);
	set_string_bla(fdsd, 32);
	aaa = floor(111);
	{
		blabla = "gfd";
		blabla = "success!";
	}
	return 0;
}
