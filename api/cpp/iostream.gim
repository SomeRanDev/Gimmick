# =================================================
# * IOStream
#
# Quick bindings for the C++ standard <iostream>.
# =================================================

@globalAll

start namespace std

@if(cpp) extern class ostream:
	op <<(input: string);

@if(cpp) extern var cout: ostream;
@if(cpp) extern var cerr: ostream;
@if(cpp) extern var clog: ostream;
@if(cpp) extern var endl: string;

end namespace
